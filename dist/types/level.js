/**
 * BaseCredLevel — Tiered score with semantic level interpretation.
 *
 * Per BASECRED_TIER.md:
 * - value: Raw numeric score from upstream protocol
 * - level: Human-readable level label derived from thresholds
 * - levelSource: Whether level came from protocol or SDK derivation
 * - levelPolicy: Versioned identifier (e.g., "ethos@v1", "builder@v1")
 */
export {};
